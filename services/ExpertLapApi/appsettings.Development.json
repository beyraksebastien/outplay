{
  "ConnectionStrings": {
    "// NOTE": "Local dev only. Point this at a local Postgres instance, e.g. via `docker run -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16`. Never commit a real production connection string here — production reads ConnectionStrings__Default from the environment.",
    "Default": "Host=localhost;Port=5432;Database=expertlap_dev;Username=postgres;Password=postgres"
  }
}
